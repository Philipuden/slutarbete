==slim :recipe_nav

-p session[:results]

h2 Sökresultat
ul
  - if session[:results].nil? || session[:results].empty?
    li Inga resultat hittades för sökningen.
  - else
    - session[:results].each do |result|
        li 
            a href="recipe/#{result['recipeId']}" #{result["name"]}